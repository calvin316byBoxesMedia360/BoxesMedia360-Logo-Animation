name: Render Remotion Video

on:
  push:
    branches: [main]
  workflow_dispatch:  # Permite ejecutar manualmente

jobs:
  render:
    runs-on: ubuntu-22.04
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Render video
        run: npx remotion render HolographicParticles out/holographic-particles.mp4

      - name: Upload rendered video
        uses: actions/upload-artifact@v4
        with:
          name: holographic-particles-video
          path: out/holographic-particles.mp4
          retention-days: 30
